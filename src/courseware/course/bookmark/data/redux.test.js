describe('Data layer integration tests', () => {
  describe('Test addBookmark', () => {
    it('should fail to create bookmark in case of error', () => {

    });

    it('should create bookmark and update model state', () => {

    });
  });

  describe('Test removeBookmark', () => {
    it('should fail to remove bookmark in case of error', () => {

    });

    it('should delete bookmark and update model state', () => {

    });
  });
});
